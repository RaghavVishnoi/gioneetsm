<ul class="nav custom-nav">
	<li class="mobile-search active">
		<a class="btn-custom">
			<i class="fa fa-search"></i>
		</a>
		
		<div class="drowdown-search">
 				<div class="form-group">
					<input type="text" class="form-control" placeholder="Email / Location code..." value="<%=params[:search]%>" id="search">
					<i class="fa fa-times nav-search-icon"></i>
				</div>
 		</div>
		
	</li>
	
</ul>
 
<script>
	$("input").keyup(function(event) {
		var search="";
		search = $(this).val();
		console.log('keypress'+search.length);
			if(search.length >= 1){
				console.log('if part');
	    	$('.nav-search-icon').css('display','block');
	    }
	        else
	        {
		        $('.nav-search-icon').css('display','none');
	        	location.href='/users'
	        }
	    if(event.which == 13) {
	         console.log(search);
 			if(search == '' || search == null){
				alert("please enter a value!")
			}else{
				console.log(search);
 				role = $('#roles option:selected').val();
				if(role != '' && role != null && role.length != 0){
				
					location.href = "/users?role="+role+"&search="+search
				}else{
					window.location.href = "/users?search="+search
				}
				
			}
	    }
}); 
	$('.nav-search-icon').click(function(event){
		location.href='/users'
	});
	
</script>